{
  "name": "peoplesearch",
  "version": "1.0.0",
  "description": "* Create a number of variants of the html + the js-file (now named index.html + main.js), so that each html-file has it's own js-file and functions differently. All pages shall use the same css file",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
